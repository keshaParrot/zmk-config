/ {
    aliases {
        uart0 = &uart0;
    };

    chosen {
        zephyr,console = &uart0;
    };

    &uart0 {
        current-speed = <115200>;
        status = "okay";
        rx-pin = <6>;  /* P0.06 — RX від STM32 TX */
        hw-flow-control;
    };
};
